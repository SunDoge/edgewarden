# Optional local-only convenience. Create apps/web/.env.local and keep it
# uncommitted. The credentials still pass through the real login/JWT flow.
VITE_DEV_AUTO_LOGIN=false
VITE_DEV_EMAIL=dev@example.test
VITE_DEV_PASSWORD=
